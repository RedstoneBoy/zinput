devices {
    in: [ljoy, rjoy],
    out: out,
}

ljoy:update {
    let lmask = 2447088;
    let rmask = !lmask;
    out.buttons = out.buttons & !lmask | ljoy.buttons;
}

rjoy:update {
    let lmask = 2447088;
    let rmask = !lmask;
    out.buttons = out.buttons & !rmask | rjoy.buttons;
}