device out;

ljoy {
    let lmask = 2447088;
    let rmask = !lmask;
    out.buttons = out.buttons & !lmask | ljoy.buttons;
}

rjoy {
    let lmask = 2447088;
    let rmask = !lmask;
    out.buttons = out.buttons & !rmask | rjoy.buttons;
}