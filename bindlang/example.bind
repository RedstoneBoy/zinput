device out;

ljoy {
    let lmask = 243294u64;
    let rmask = !lmask;
    out.buttons = out.buttons & !lmask | (ljoy.buttons & lmask);

    let x = 432.42;
    let y = 521.1;
    x * y * x;
    x * y / x;
    x / y / y / x;
    x >= 2.0;
}